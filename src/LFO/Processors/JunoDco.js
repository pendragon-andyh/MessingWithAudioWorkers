export class JunoDco{
  // Public.

  // Private (and for testing).

  // Tuning.
  sawtoothTarget=1.2;

  constructor(sampleRate) {
    this.sampleRate=sampleRate;
  }

  setNote(noteNumber) {
    
  }

  process(lfoValue, bendValue) {
    
  }
}